# InkKeep · 胶印 RIP 油墨键预设与 TAC 限制器

## 这不是什么

不是 Wiki、不是 CMS、不是博客。页面上的「记录」是**一块印版分色**，不是文章。

## 谁在用

中小型商业印刷厂的预印车间。CTP 出片前，操作工要把 PDF 分色成 C/M/Y/K（及专色），算出每色网点覆盖率，再把油墨键开度送到海德堡/小森墨控台。总覆盖率（TAC, Total Area Coverage）过高会在铜版纸上糊版、粘脏。

## 核心业务

1. 每条记录 = 一联印版：标题是印件工单号，正文是各色覆盖率（`C=.. M=.. Y=.. K=..`），标签是色版名。
2. **TAC 硬限制**：C+M+Y+K+专色 不得超过 320%（铜版纸车间默认；不在本产品做「预约/排产」）。
3. 专色必须具名（如 `Pantone185`），覆盖率单独计入 TAC。
4. 附件是该分色的 TIFF/ICC，路径必须落在数据根下，禁止 `../` 逃逸。
5. 修订历史保留每次墨键改动，便于夜班对回白班的预设。
6. 导出时走流水线：校验 TAC → 建索引 → 打包墨键表（zip+manifest）。

## 运行与验收

- 控制台：`go run ./cmd/server`，浏览器打开首页可登记印版、搜工单。
- `GET /healthz` 必须 200；`POST /api/records` 在 TAC>320 时拒绝。
- 本仓只修 Go 缺陷；页面只作可运行外壳。
